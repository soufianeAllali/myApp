[phases.setup]
nixPkgs = ["php82", "php82Packages.composer", "php82Extensions.gd"]

[phases.install]
cmds = ["composer install --no-interaction --optimize-autoloader"]

[start]
cmd = "php artisan serve --host=0.0.0.0 --port=$PORT"